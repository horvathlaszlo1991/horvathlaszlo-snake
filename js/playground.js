class Playground {
  constructor(width, targetDiv) {
    this.width = width;
    this.targetDiv = targetDiv;
    this.generate();
  }

  generate() {
    for (var i = 0; i < (this.width * this.width); i++) {

    }
  }
}
